from __future__ import annotations


IN_APP_GUIDE_TEXT = """第一次打开，先选一种入口

【从 Aspen 文件开始】
把一个 .bkp、.apw 或 .inp 文件拖到左侧浅蓝区域，也可以点“选择文件”。拖入只会选中文件，不会马上运行 Aspen。

压力基准一般按项目原来的设置选。当前 Aspen 案例使用绝压时选“绝压”；只有确认输入是表压时才选“表压”。所有展开框显示中文，程序内部仍保存可复算的标准代码。表压算压比、或绝压换成设计表压/标准表压上限时要填当地大气压；程序不代填，没填只把依赖换算的步骤标成待补。勾选“重新运行并采集原始历史证据”，程序才会用隔离副本重新运行并保存本次 .his 证据。大案例建议把超时改为 1800 秒。

确认文件后，点“自动遍历并匹配”。源文件不会被覆盖；程序会复制一份，在独立进程中读取模块、流股、单位、运行状态和连接。

【没有 Aspen 文件】
打开“02 手动输入”，先选设备或 Aspen 模块类型。字段右侧的“必填 / 建议 / 可选偏好 / 已有结果”说明它在当前算法里的角色；材料通常是可选偏好，不是必须输入。鼠标移到每行的 ⓘ，会临时说明来源、什么时候要填、留空影响哪一步以及会触发哪条公式。一个框只填标签对应的一项；有单位时按旁边单位填写，不知道的不要猜。

点“计算并匹配”后，软件会先算能算出的参数，再给设备型式、候选和缺口。缺少一个量时只停止依赖它的步骤，不会丢掉其他已知结果。

【怎么看右边的结果】
PFD 流程图：左键设备看完整参数；在参数卡或右键菜单点“补充/修改本设备参数并重算”，可按设备位号补录 Aspen 没有的设计参数。补录窗口每一行都有 ⓘ，会说明已有 Aspen/现有值、单位、公式和证据边界；留空就沿用现有值。补录值只存独立层，确定后只重算本设备，关联管线和直接上下游仍标为待复核；清空补录可恢复原 Aspen/已有输入。右键仍可临时修改模块对应的设备类型，选 AUTO 可恢复自动识别。任何人工补录或改型都不会改 Aspen 文件，也不等于厂家最终型号或正式证据。

推导流程（可修改）：六个可点击框依次表示“数据来源 → 模板 → 计算 → 型式/材料/部件 → 候选与非标修改 → 客户交付”。点框后左侧显示程序默认值和当前场景值。可选择的项目使用中文展开框，内部代码只在详情中用于审计。修改后先保留为人工场景，点“仅重算当前设备”才执行完整重算；点“恢复程序默认”可撤销本设备全部覆盖。程序始终保留原默认值、人工修改项、重算结果和各自 SHA-256。人工修改不能绕过正式证据门，也不会修改 Aspen 源文件。

客户交付：按客户表格需要的字段查看哪些已经有、哪些还缺。

分支选择：先用自然文字显示程序实际选择的设备族、型式、规格候选和非标/多台方案，再逐条显示判断条件是满足、不满足还是未知。泵、塔、换热器等主设备与法兰、密封面、垫片、紧固件、阀门、管件等连接口部件都必须写明程序所选内容、走过的规则、被排除的候选、缺少的条件和来源；不能只给一个没有背景的结果字符串。

大模型调控：单独显示本次是否调用、使用的 provider/model、模型摘要、条件判断、补值建议、终选分支建议、审核发现、程序校验结果以及真正带入重算的内容。模型调用失败时显示回退原因；模型建议没有通过白名单或硬门时也照样显示，但明确标成“未应用”。

参数卡：看输入值、计算值、单位和来源。“内置公式计算 ⚠”表示该值不是 Aspen 或用户直接输出。

候选型号：看当前能够支持到哪一级。标准标记候选不是厂家最终型号；非标塔器、容器和反应器通常只给工程规格。

当单台换热面积过大、泵工况落在奇异区域、塔体筛选尺寸过大或单台标准路线不合适时，程序会给出具体的多台/串并联修改方案、估算台数、单台目标工况和系统标记，并显示醒目的“算法初筛”警告。该方案不是国标覆盖证明、厂家曲线、EDR、塔内件水力学或正式机械设计，必须按结果中的固定复核动作继续闭合。

终选型式来源：“已指定型式”表示用户或同工况明确给出；“条件选定”表示登记条件已命中；“默认选定”表示条件不足时采用通用登记型式。默认型式不是厂家最终型号，但不会再用“待定/其他/或”拖住后续计算。

校核与缺口：这里列出下一步真正需要补的参数、软件结果或厂家证据。

公式链：每条都按“目标量 = 公式 = 代入式 = 答案”显示。黄色提示说明公式的适用范围；悬停 ⓘ 可看“不能证明什么”。

机器 JSON：给脚本或 Agent 读取，不需要人工抄界面。

Agent 组织答案：固定按“基本信息、分支选择与大模型调控、详细计算链条、候选与系统修改方案、强制警告、待补证据、下一步”排列。大模型的自然语言结果会随分支账本输出；只有通过程序白名单、校验和确定性重算的建议才会影响当前值，大模型不能改程序硬门、伪造厂家型号或关闭 OPEN 门。

需要留档时点“导出报告”，可保存网页、Markdown 或完整 JSON。程序同时生成 `.manifest.json` 哈希清单，绑定源结果、组织答案和报告文件。

【关于大模型】
不用配置大模型，主要计算和匹配也能运行；无 API 时每台物理设备仍会落到唯一登记型式并显示默认/条件/显式来源。接入 API 后，选择“设备型式、材料与零部件受控选择”注入点，大模型会收到当前设备族的背景、允许选择的具体型式和材料/零部件组合、每个选项的触发条件、选择依据、来源、已有值、冲突项和上下文哈希。它只能返回登记的规则 ID、条件 ID、选择轴 ID、选择项 ID 和原上下文哈希；程序复核后只补空缺字段并单设备重算。虚构规则、自由文字型式、表外材料或与用户/Aspen 已有值冲突的选择不会应用。真正应用的 AI 选择会以 J 级暂定依据、TYPE_SCREENING 用途上限和“未覆盖已有字段”记录输出。大模型不能改已有计算值、单位、压力基准、证据状态或厂家最终型号。第一次使用可以先不管这一页。

桌面“Agent 协同”页先使用本次 Aspen 或手动匹配的确定性结果，不是独立的数据导入入口。填写“服务接口”、Base URL、精确模型 ID 和 Key 后，先点“测试连接”；看到连接成功后点“应用设置”，最后才点“开始协同计算”。服务接口、注入点和上下文范围均显示中文，内部请求仍使用已登记的标准代码。任何设置改动都会使已应用状态失效。勾选框文字中的“勾选=启用”就是开启该功能；不想使用大模型时取消勾选，应用后仍可运行确定性模式。测试连接会向所选模型发送一次最小请求，失败时显示脱敏后的具体原因；Key 只留在当前进程内存，不写入结果或日志。无界面 Agent/CLI 才能直接提交 Aspen 或手动 JSON 请求。

【查询知识图谱】
先在“KG 知识图谱”页选择设备族，再从字段目录选择中文字段；软件会同时显示 canonical ID、单位和输入/证据定位，并自动生成查询。用户不需要预先知道内部字段名。也可以直接输入自然语言，最后统一点击“开始查询”。

连续处理多台设备时，用 EquipmentDesignAgentCLI.exe --session-jsonl，让调用脚本逐行发送请求和读取响应。这样程序、设备目录和知识资产只加载一次；每台设备仍单独计算并返回自己的状态，某一条失败不会污染下一条。

【关于内置公式】
程序允许用内置公式计算可闭合参数，但会明确报警告。定义式、几何式和守恒式也会标明“内置公式”；含设计分支或初筛假设的结果固定为暂定，不能直接升为最终型号。

程序允许使用已登记的预设计推荐公式和最终保底值，例如密度、效率、流速、停留时间、装填系数、材料路线以及换热面积所需的 U、LMTD、F 初筛组合；每一个都会显示来源、假设、敏感性和 `J/provisional` 警告，不会伪装成 Aspen/用户值，也不能升级为厂家最终值。同工况明确值一到位，程序会优先采用并重新计算。

泵会按介质与工况分支，明确输出泵壳、叶轮、轴、轴套、机械密封、辅助密封和垫片的程序预选；串联方案会逐级累计关死压力，给出末级最大出口表压、法兰 PN 和 GB/T 5662 的 16 bar 范围校核。没有厂家关死扬程时采用登记保守系数并报警告。GB/T 5662 的额定参考点不能代替厂家完整 Q-H、效率、功率和 NPSHr 连续曲线；后一项在设备一览表中单列，未提供就保持 OPEN，但不会再把材料选择和承压计算整体推给用户。

Q=0 不会被当成“零面积换热器已经完成”；面积链保持待核实，但仍保留最泛用换热器工程规格候选。design_pressure_mpa 输入框只显示中性的 MPa，直接值必须说明绝压或表压；绝压先按当地大气压规范化为表压，表压小于或等于零时进入外压分支，不继续套内压厚度式。

已选 volume_m3 必须说明 volume_basis。required total 与直筒段几何容积分开：名义总容积和几何总容积直接与最低总容积比较；有效工作容积与“最低总容积 × 装填系数”比较。液透平的 ΔP/(ρg)、ΔP·Q 和再乘效率，只是压差水头分量、压差功率分量和轴功初筛，不是全机总轴功或最终选型。

塔的 πDi²/4 是全筒截面积，尚未扣降液管、受液板和无效区。膜面积只有 cylindrical_channels 使用内置几何式；其他几何要给同工况外部 membrane_area_m2。材料默认可见，但属于可选偏好；留空时程序保留泛用候选和材料证据缺口。

封头厚度的内置分支目前只实现显式选择的 head_type=2:1_ellipsoidal；其他封头需给对应外部计算结果。泵的 NPSH 数值差只有在显式给出 required_npsh_margin_m，并由同工况厂家曲线、npshr_evidence_scope=same_duty_vendor_curve、证据清单和独立审核共同闭合后，才会标为 PASS。

【常见情况】
文件拖不进去：点“选择文件”仍可正常使用，并确认文件扩展名是 .bkp、.apw 或 .inp。

导入很久没结束：大案例可能需要十几分钟；先看超时是否太短，不要连续点击启动多个任务。

结果里没有最终型号：先看“校核与缺口”。缺少同一台设备的厂家曲线、计算书或审核批准时，软件会停在设备型式、标准标记候选或非标工程规格，这是正常的证据边界。

封头型式或 NPSH 仍待补：封头厚度需要明确公式分支；NPSH 需要规定裕量和同工况厂家曲线。只有一个数字或文件名不会自动升级为通过。

流程逻辑节点没有型号：FSPLIT、MIXER、HIERARCHY 默认只是模拟连接逻辑，不会被硬说成一台物理设备。

【结束使用】
需要留档时点“保存 JSON”或“导出报告”，也可以打开证据目录。确认任务已经完成后直接关闭窗口。若后台仍在处理，程序会先询问；确认强制关闭时只终止本软件本次创建的 Aspen 隔离工作进程，不会关闭你原来已经打开的 Aspen。
"""


__all__ = ["IN_APP_GUIDE_TEXT"]
