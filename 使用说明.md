# 设备设计图谱与自动选型程序

本程序把 Aspen 工程文件中的流程数据、PFD 连接关系、设备计算、设备型式候选和管线候选组织为一条可追溯的确定性链路。

## 运行程序

- 图形界面：运行 `EquipmentDesignGraphApp\EquipmentDesignGraphApp.exe`。
- 命令行/自动化：运行 `EquipmentDesignAgentCLI\EquipmentDesignAgentCLI.exe --help`。
- 发布包采用目录形式；`_internal` 中包含完整知识库、SQLite、Tcl/Tk 和源码权威快照。移动程序时必须复制整个 `EquipmentDesignGraphApp` 或 `EquipmentDesignAgentCLI` 目录，不能只拿 EXE。
- Aspen 直接导入需要本机已安装可用的 Aspen Plus；程序始终在新的暂存目录中打开副本，不覆盖原始工程。
- 导入时必须明确选择压力基准。表压工程还必须输入当地大气压，程序不会暗自采用默认值。

图形界面支持拖入 `.bkp`、`.apw`、`.inp`、`.json`、`.csv` 和 `.xlsx`。`.apwz` 不是当前受支持格式，会明确拒绝而不是假装成功。

在 Aspen 页可以选择单个工程，也可以：

1. 点击“添加多个 Aspen 文件”建立队列，或载入带路径、压力基准和预期 SHA-256 的 JSON 清单；
2. 选择是否重新运行、是否在隔离副本中补充流股输运物性；
3. 点击“严格串行批量运行”；程序逐案显示状态和进度，单案失败后继续后续案例；
4. 双击已完成案例，把该案的设备、管线、PFD 和计算链载入主界面；
5. 查看 `aspen_suite_report.json` 和 `aspen_suite_report.md`。汇总报告分别列出可继续选型、正式工艺基础、物性门禁、具体候选覆盖和源文件哈希状态。

## 自动链路

1. 校验源文件、计算 SHA-256，并建立隔离副本。
2. 读取 Aspen 的工况、模块、物流、单位、连接关系、运行历史和证据文件。
3. 把常见 SI、工程制和英制单位规范化到统一计算单位，并保留原值、原单位和换算式。
4. 生成设备和管线的规范输入、参数谱系、计算式、代入值、结果和阻断项。
5. 在 17 个设备族中匹配设备，输出具体工程型式、标准规格候选或部件规格。
6. 输出 PFD、设备明细、管线明细、证据边界、正式使用门槛和机器可读结果。

Aspen `MIXER`、`FSPLIT`、`HIERARCHY` 默认只表示流程逻辑节点，不会凭空创造物理设备；它们在结果中明确标为 N/A。Aspen `PIPE` 是物理水力管段，会确定性路由到工艺管道选型。

## “具体候选”与“最终型号”

程序不会再使用“非标准型”“通用型”“待定型”或占位符作为推荐结果。

- 标准泵样本可以输出类似 `GB/T 5662-2013 65-40-250 @ 2900 r/min` 的标准规格候选。
- 自制设备会输出具体设备型式及已闭合的工程规格。例如塔器输出“单溢流筛板塔”和 Aspen 级数；正式塔径、塔高未闭合时明确显示 `Di_formal=OPEN`、`H_formal=OPEN`，内部初筛尺寸不会冒充正式几何。
- 管线会输出“无缝钢制工艺管道”或“直缝埋弧焊钢制工艺管道”，并列出 DN、外径×壁厚、PN、材料、起终点、流速、雷诺数和单位长度压降；应力与支吊架未完成时为明确 OPEN。
- 管件、法兰和垫片输出具体部件规格。

工程候选不等于厂家最终型号。缺少同设备机械设计、材料、外压校核、专用软件、厂家曲线或批准证据时，结果会继续保持 provisional/blocked，不会被升级为正式型号。

## 非标、大型和奇异工况的程序修改方案

程序在确定性计算之后执行固定的工程修改策略：

- 大换热器会同时给出受限并联列数的模块化主方案、全并联比较方案和全串联比较方案；每个方案均有单台面积/热负荷、材料、管径、管长、管数和程序工程规格，并明确总量守恒尚未证明温度程序、压降或流量分配等价；
- 普通泵工况保持单泵，不会只为贴近目录点而拆成多泵串联；高流量低扬程、高流量中扬程和高扬程分别进入轴流、立式导叶式混流和内部多级泵路线，输出运行/备用/安装数量、单机 Q/H、内部级数、材料/密封和 PN。非轴向吸入泵不会误套 GB/T 5662 标记；
- 大塔会估算并列塔数量和负荷分配，正式塔径、塔高及塔内件水力学继续保持 `OPEN`；
- 没有安全自动配置时，结果会给出专项评审路线和固定的下一步动作，而不是写“非标准型”。

所有上述结果固定为 `J / provisional / TYPE_SCREENING`，并显示“纯算法初筛”警告。台数、串并联方式和系统标记必须由厂家及工艺、机械、管道专业按同一工况复核。程序会在“分支选择（自然文字）”中说明实际命中的泵型/换热器构型分支，并逐项列出等价比较方案和未闭合风险。

即使 Q/H、换热负荷、U、LMTD、压力、温度或材料缺失，程序也会用登记默认值或内置公式生成完整的程序候选，不再只显示“待计算”或“厂家型号待定”。所有退化字段、采用值和来源均逐字段标注，用户补值后应执行单设备重算；该完整候选仍不能直接采购、施工或报审。

## 图形界面导航与快捷操作

顶部流程条按“输入 → 确定性计算 → 审核与调整 → 导出”显示当前阶段。右侧结果不再把所有内容铺成同级页签，而是按任务分为四组：

1. **流程**：PFD 和设备/管线入口；
2. **选型结果**：客户交付、分支选择、参数卡和候选型号；
3. **计算与校核**：可修改推导、校核缺口和公式链；
4. **Agent 与数据**：大模型调控、Agent 组织答案和机器 JSON。

PFD 中左键设备会先显示参数/证据详情，再打开已预填当前有效值的可修改输入框；保存时只记录相对
Aspen/程序基准的差异并重算本设备。右键只用于选择设备类别或恢复自动识别，选好类别后不会立即
计算，须再左键检查和补充该类别参数。类别或参数变化影响到的直接上下游设备与管线会继续标为待复核。

设备类别或参数一旦改变，改型前的 Agent 基线立即失效。再次启动 Agent 时，运行前硬门必须同时确认：
当前节点存在有效 `selection`、来源状态为 `RECALCULATED_CURRENT`、确定性结果哈希与当前选择哈希一致。
任一项不满足，程序会要求先重算当前设备，而不会让旧回答写回新设备型式。

手动输入页可按中文名、字段 ID 或分组搜索，并可切换“全部默认字段 / 只看主计算必填 / 只看待补字段 / 只看已修改”。顶部缺口卡会用文字和颜色共同显示主计算、候选闭合和正式证据三层状态；缺少主输入时，运行仍按登记降级链生成结果，并把焦点定位到首个待补字段。

常用快捷键：`Ctrl+O` 选择 Aspen 文件，`Ctrl+Enter` 执行当前入口的主操作，`Ctrl+F` 定位字段筛选，`Ctrl+S` 保存 JSON，`Ctrl+Shift+S` 导出报告，`Ctrl+R` 仅重算当前设备，`Alt+1`～`Alt+4` 切换四个输入入口，`F1` 打开内置说明。快捷键与按钮调用同一逻辑，不绕开校核或证据门。

## 可视化推导链与单设备重算

图形界面的“推导流程（可修改）”包含六个可点击节点：

1. 数据来源与工况；
2. 设备族与计算模板；
3. 公式计算与交叉核对；
4. 型式、材料与部件选择；
5. 型号候选与非标修改；
6. 客户交付与证据门。

每个允许修改的字段同时显示“程序默认值”和“当前场景值”。模板、相态、压力基准、设备型式、Agent 服务接口、注入点和上下文范围等选择项均使用中文展开框，内部稳定代码仅用于重算和审计。用户修改后可只重算当前设备，也可恢复本设备全部程序默认值。

人工场景不会覆盖 Aspen 源文件；程序会记录基线结果 SHA-256、每项默认值/人工值、重算结果 SHA-256 和覆盖审计 SHA-256。人工输入本身不能升级正式型号或绕过证据门。

## 报告导出与 Agent 组织答案

结果页可导出 HTML、Markdown 或完整 JSON 报告，并同时生成 `.manifest.json` 哈希清单。报告绑定源确定性结果、展示对象和组织答案的 SHA-256。

### 怎样检查内置公式不是黑箱

在结果页打开“公式计算与追溯”区域，或查看导出的 HTML / Markdown 报告。每条已经执行的公式会显示：

- 公式 ID、表达式、适用范围和禁止声称；
- 本次实际输入值、单位、输入来源类型及字段值 SHA-256；
- 上游计算 ID 与上游公式追溯 SHA-256；
- 知识来源文件、锚点、定位行、源文件 SHA-256 和绑定状态；
- 执行源码路径、源码文件 SHA-256、当前清单纳管源码集合 SHA-256 和清单状态；
- “公式定义 SHA-256”与“本次计算追溯 SHA-256”；
- 尚未绑定的输入来源、外部标准原文或其他追溯缺口。

两个哈希用途不同：公式、来源或执行代码变化时，公式定义哈希变化；只修改流量、压力、密度等本次输入时，公式定义哈希保持不变，本次计算追溯哈希会变化。显示 `REPRODUCIBLE_TRACE_WITH_OPEN_PROVENANCE` 表示公式和代码可复算，但仍有输入来源或外部证据未闭合，不能当作正式工程验证完成。

Agent 固定按以下顺序组织回答：

1. 结论；
2. 计算；
3. 候选与系统修改方案；
4. 强制警告；
5. 待补证据；
6. 下一步。

接入大模型后，大模型也只能在登记边界内选择、解释和整理语言，不能更改程序给出的计算值、设备台数、串并联方式、型号/系统标记、警告或 `OPEN` 证据门。命令行可使用 `render_report` 导出报告，或使用 `organize_answer` 单独取得组织后的答案。

在“03 Agent 协同”页接入模型时，主要方式就是由用户依次填写服务接口、API Base URL、精确模型
ID 和仅供本次进程使用的 API Key，再选择服务实际支持的协议。兼容服务通常选择
“Chat Completions（传统兼容）”；明确支持 OpenAI Responses 的服务才选择
“Responses（推理模型）”。Responses 可设置推理强度，需要最高强度时选“超高（xhigh）”，并默认
保持“禁止服务端存储响应”勾选。任意自定义 URL 应选择“OpenAI 兼容接口”；OpenAI 和 DeepSeek
官方接口使用固定地址，避免测试连接与实际 Agent 请求落到不同路径。

先点“测试连接”：程序会把当前 URL、Key、模型 ID、协议和推理模式组成一次最小 JSON 功能请求，
并实际解析 `status=pong`，不是只检查 URL 格式或 HTTP 200。成功后点“应用设置”，最后才能开始
协同计算并查看模型回答。自定义 API 根地址和已经带
`/chat/completions` 或 `/responses` 的完整地址都可以输入；程序按协议规范化一次。任何 URL、协议、
模型、推理强度、存储开关或密钥变动都会使旧测试和已应用设置失效，必须重新测试。Agent 运行期间
配置或确定性输入变化时，旧回答会被丢弃；认证请求不跟随 HTTP 重定向，Key 仍只驻留当前进程内存。

### 2026-08-20 远程连接实测

DeepSeek 官方 URL、用户 Key、官方模型目录和连接探针均已实际验证成功。较早一次 bridge 级测试中，
泵、管壳式换热器、真空塔、压缩机、工艺管道和调节阀六个无项目敏感数据的合成流程曾完成 6/6。
这项历史证据仍保留，但它不是本次完整 GUI 复验结果。

最新严格网络复验中，前 5 个合成设备均严格通过；执行第 6 个调节阀时，官方服务返回 HTTP 402
`Insufficient Balance`。余额耗尽后继续执行 GUI 真空塔流程也被同一 402 阻断。因此当前结论是
“官方配置和前五条设备链通过，完整 GUI 网络矩阵因余额不足未完成”，不能写成“本次 GUI 6/6”。

本次实测不表示远程模型取得了工程权威，也不表示所有兼容 URL、模型和未来时段均可用。两份真实 Aspen
派生案例只在本机运行，没有发送给远程服务；它们都得到具体初选候选，同时继续显示厂家/机械正式证据
缺口。API Key 没有写入项目文件、测试输出、报告或运行清单。完整矩阵和边界见
[v2.4.3 发布与成品核验说明](docs/RELEASE_2_4_3_20260820.md)。

已发布 v2.4.2 的冻结源码在发布前回归中为 499 项通过、1 项按环境条件跳过，当时受控 AI 选择与编排
全族矩阵为 45/45。这是 v2.4.2 的历史验收证据；v2.4.3 当前源码全量回归为 542 项通过、1 项按环境条件跳过。后续重新做
网络验收时，审计脚本应优先带 `--tk-smoke`：先执行真实 Tk 窗口、PFD 改型与运行前基线硬门检查，
本地状态通过后再调用远程模型，避免把调用额度消耗在可提前发现的 GUI 问题上。

源码维护者应先执行 `python -m pip install -r requirements-test.txt`；该测试入口复用完整运行/构建
依赖，JSON Schema 校验器属于 Aspen 覆盖旁车的运行依赖。v2.4.3 构建脚本会在生成 CLI 后自动执行打包内的
`agent_selftest_request.json`；进程退出非零、响应 `ok` 非真或结果状态不是 `PASS` 时不会产出可交付构建。

## 当前第一阶段验收状态

确定性派生引擎 1.9.5 在 Aspen V14 上新增完成 S01–S06 六案实测：

- 6/6 案例可继续设备选型，6/6 原始 BKP 的运行前后 SHA-256 相同；
- 6/6 物理设备均有具体候选，19/19 物理管线均有具体管道规格候选；
- S02、S03、S04 的原始历史为干净运行证据；S01、S05、S06 仍有警告或物性阻断，只能继续初步选型，不能作为正式工艺基础；
- S06 的 L-PRO 没有正流量、相态、压力和温度时，程序明确输出 `DN25 / φ34×4 mm / PN16 / 20钢 / 对焊 / CS20-PN16-BW-CA1.5` 的最低完整占位分支，并显示禁止采购、施工和报审的最高级警告。

逐案证据见 `docs\ASPEN_SMOKE_SUITE_VERIFICATION_20260728.md`。

早期 1.9.3 版本还用 CUMENE、EX2_1、EX2_4、MCH 四个真实 BKP 验证：

- 19 台普通物理设备、2 个 Aspen PIPE 物理块、37 条独立 PFD 管段，共 58 个物理对象；
- 39 条唯一物理管线，4 个 PIPE 端点别名已归并而未重复计数；
- 每个物理对象均有程序生成的具体型式或工程规格候选；
- 58/58 行客户全字段结构通过，未知值只允许使用 `value=null` 的明确 OPEN；
- 模型候选门禁为 PASS，详细逐行门禁为 PASS，客户一览表为 `PASS_WITH_EXPLICIT_OPEN_GAPS`；
- 正式机械/厂家设计仍为 BLOCKED，不能把第一阶段候选写成最终设计。

详细证据见 `docs\STAGE1_REAL_BKP_RELIABILITY_REPORT_20260724.md`。

客户表采用“行级完整来源链 + 单元格哈希指针”。这既保留逐字段验签，也避免把相同 Aspen 运行历史和来源正文复制到每个单元格。

## 黏度与管线水力学

管线优先使用 Aspen 导出的 MUMX。仅当单相黏度确实没有抽取到、且存在适用范围明确并与源文件绑定的相关式记录时，程序才允许把内置相关式结果用于雷诺数和摩阻初筛：

- 只能用于单相初步水力学；
- 必须标记 J/TYPE_SCREENING；
- 必须显示“不是 Aspen 提取值”的警告；
- 两相、未知相、含固相和超临界状态不允许把默认值冒充相关式结果或 Aspen 物性。

若黏度受阻，程序可使用登记的水力学默认参数包形成带警告的管道候选；若连正流量和工况状态也缺失，则使用 DN25 最低完整规格占位分支。两种退化都必须显示采用字段、默认档案、适用边界和 `TYPE_SCREENING`，雷诺数、严格摩阻和正式管径验收保持 OPEN。

## 泵功率与转速语义

泵会分列流体水力功率、轴功率和电功率，并分别检查“流体→轴”和“轴→电”两套平衡。缺少驱动效率时，第二套平衡保持 OPEN。

Aspen 历史中的 `ACT-SH-SPEED` 只有在证据明确证明为最终求解结果时才能作为实际轴速。若它来自输入 deck 或 DESIGN-SPEC 搜索边界，只能写入“配置/设计规格候选转速”，不能覆盖实际轴速。标准候选参考转速也必须单列。

## Aspen 运行状态

- `CLEAN_RUN`：Aspen 原始运行历史未发现 terminal/severe/error/warning 计数。
- `DIRTY_RUN`：仍可抽取信息并做初步选型，但正式工艺基础被阻断。
- `ELIGIBLE_AS_PROCESS_BASIS`：仅表示可以作为当前流程计算基础，不表示机械设计或厂家型号已经完成。
- `PROVISIONAL_NOT_FORMAL_PROCESS_BASIS`：只能作为初步结果，报告会列出精确阻断原因。

## LLM 辅助

LLM 位于确定性计算之后，只能做受约束的补充分析：

- 不能改写确定性数值、单位、公式、证据哈希、候选注册表或最终型号门槛。
- 只能引用程序提供的候选 ID、条件 ID 和计算配方。
- 合法 ID 仍必须通过确定性“触发证据门”：空字段本身不构成材料、密封或结构路线的适用证据；缺少该分支登记条件时，建议被拒绝而不写回。
- Aspen 与用户已给值优先；Agent 只能补充允许的空字段，不能用另一种材料或部件覆盖现值。
- 模型估算只能填补允许的缺失字段，必须标记为 J 类、provisional、`TYPE_SCREENING`。
- 写回前必须经过结构校验、哈希复核、重新计算和用户明确批准。

未配置远程模型时，确定性导入、计算、选型、PFD 和报告仍可完整工作。开发验收可以使用受控模拟模型验证 `hybrid_prepare → provider → hybrid_continue`，但模拟结果同样不能获得确定性权威。PFD 单设备保存并重算后，Agent 绑定的是该节点最新的确定性结果与输入哈希；运行前还必须通过 `selection + RECALCULATED_CURRENT + hash` 三项硬门，不再审核或写回先前的整份 Aspen 导入结果。若节点又被修改，旧模型回答会失效。

## 命令行示例

```powershell
.\EquipmentDesignAgentCLI.exe --bkp "D:\case\example.bkp" `
  --pressure-basis absolute `
  --output-dir "D:\case\run_001" `
  --run `
  --output "D:\case\run_001_response.json" `
  --pretty
```

使用已有 Aspen 结果而不重新求解：

```powershell
.\EquipmentDesignAgentCLI.exe --bkp "D:\case\example.bkp" `
  --pressure-basis absolute `
  --output-dir "D:\case\read_001" `
  --no-run `
  --output "D:\case\read_001_response.json" `
  --pretty
```

批任务通过 Agent JSON 调用 `aspen_suite`。`manifest_path` 指向 BKP 清单，`output_dir` 必须是新的或空的目录；`require_all=true` 时，只要任何物理设备或管线没有具体候选，整个请求就返回失败。

## 可靠性复核

交付目录提供三层复核：

- `scripts\audit_multi_bkp_model_gate.py`：具体候选与物理粒度；
- `scripts\audit_multi_bkp_overview_gate.py`：客户全字段、OPEN 合同和表级哈希；
- `scripts\audit_stage1_detailed_reliability.py`：逐设备/逐管线水力、来源、实体归并和最终行绑定。

它们共同要求：

- 每台物理设备都有确定性、非占位的具体型式或规格候选；
- 每条物料管线都有具体管道候选、详细工程规格和水力状态；
- 流程逻辑节点必须明确为 N/A；
- 单位诊断为零；
- Aspen 脏运行或计算阻断时，正式门槛不得被错误放行。

输出包括 JSON 总表、CSV 行级清单和 Markdown 摘要。

## 常见问题

- 无法启动 Aspen：先确认 Aspen Plus 能独立打开同一工程；检查许可、COM 注册和版本。
- 结果为脏运行：先在 Aspen 中处理错误/警告，再重新运行导入。
- 真空管段被阻断：需要单独的外压/失稳设计分支，普通内压壁厚公式不能替代。
- 泵只有候选而非正式型号：补充 NPSHr、完整曲线、材料和厂家证据。
- LLM 未连接：检查所选提供方的端点、模型和密钥配置；不要把密钥写入工程文件或报告。

## 交付边界

标准资料的结构化索引可用于检索和溯源，但任何尚未完成逐表/逐图复核的标准内容不得被宣称为正式完整知识库。程序会保留来源、证据等级和阻断项，工程师仍需对正式交付负责。
